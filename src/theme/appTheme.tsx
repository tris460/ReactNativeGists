import { StyleSheet } from "react-native";

export const globalStyles = StyleSheet.create({
    alignCenter: {
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
    }
})